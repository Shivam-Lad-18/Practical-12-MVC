@{
    ViewBag.Title = "Delete All";
}

<h2 class="text-danger">Delete All Employees</h2>

@if (ViewBag.Message != null)
{
    <div class="alert alert-warning">
        <strong>Warning!</strong> @ViewBag.Message
    </div>
}

@Html.ActionLink("Back to Menu", "Index", null, new { @class = "btn btn-primary" })
